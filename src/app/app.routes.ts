import { Routes } from '@angular/router';
import { ChatPageComponent } from './page/chat-page/chat-page.component';

export const routes: Routes = [
    { path: 'chat', title: "Chat", component: ChatPageComponent },
];
